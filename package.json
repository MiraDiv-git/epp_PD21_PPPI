{
  "name": "hello-world-nodejs",
  "version": "1.0.0",
  "description": "Hello World Node.js application for CI/CD demo",
  "main": "index.js",
  "scripts": {
    "test": "mocha test.js --reporter mocha-junit-reporter --reporter-options mochaFile=report.xml",
    "build": "mkdir -p dist && cp index.js dist/index.js && cp package.json dist/package.json",
    "start": "node index.js"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "mocha-junit-reporter": "^2.2.1"
  },
  "keywords": ["hello-world", "ci-cd", "nodejs"],
  "author": "Student",
  "license": "MIT"
}
